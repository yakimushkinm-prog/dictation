[Unit]
Description=Dictation transcription server (faster-whisper GPU)
After=network.target

[Service]
WorkingDirectory=%h/dictation/server
ExecStart=%h/dictation/server/.venv/bin/uvicorn server:app --host 127.0.0.1 --port 9876
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
